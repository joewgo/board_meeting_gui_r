# 🎤 露娜的 AI 董事會：多模型聯合仲裁報告 (20260418_175832)

- 會議模式：單模型（只用裁判長）

## 🖼️ 【參考圖片】

![圖片](file:///C:/Users/allmagicer/Documents/GitHub_Copilot/board_meeting_gui_r/異常圖片/螢幕擷取畫面 2026-04-18 175209.png)

---

## 📝 【使用者需求】

> 我在cpu為N100的迷你電腦執行這個程式，board_meeting_gui_v2.8.9.py會出現以下錯誤：

--- 露娜的 AI 董事會 (v2.8.9) 啟動 ---
[系統] 會議模式：討論共識（三專家→裁判長）
[系統] 未偵測到圖片，將以純文字模式進行推論。

----------------------------------------
[🟢 專家 A (claude-haiku-4.5) 正在思考與作答...]

❌ 會議發生錯誤：'str' object has no attribute 'get'

--- 會議結束 ---

用命令列執行：
python -c "import re; f=open('board_meeting_gui_v2.8.9.py','r',encoding='utf-8'); c=f.read(); f.close(); print('OK' if 'v2.8.9' in c and '\"system_message\"' not in c.split('system_message')[1][:50] or True else 'CHECK')"
  File "<string>", line 1
    import re; f=open('board_meeting_gui_v2.8.9.py','r',encoding='utf-8'); c=f.read(); f.close(); print('OK' if 'v2.8.9' in c and '\
                                                                                                                                  ^
SyntaxError: unterminated string literal (detected at line 1)




**請更新此程式為board_meeting_gui_v2.8.10.py，且若有需要則一併修正readme.md內容，
確認新版本有效後；且已是第二輪專家討論皆同意後，最終由裁判長(仲裁者)才可刪除舊版本。**

*****修改完成後，進行git add .及 git commit並 git push -u origin main 但資料夾：「異常圖片」含內容檔案皆不需commit及push，
如果push失敗則停止，改為生成報告後，由使用者手動處理。*****

---

## 🟣 【最終裁決 (claude-sonnet-4.6)】

Let me analyze the error and understand what needs to be fixed. The error is `'str' object has no attribute 'get'` which suggests somewhere in the code, a string is being treated as a dictionary. Let me look at the code first.Let me look at the area around line 580-720 where the Copilot call is made, particularly around `call_copilot_model` function:Now let me look at the `stream_agent_response` function more carefully. The error is `'str' object has no attribute 'get'`. This is happening in the `stream_agent_response` function. Let me look at how `session.send()` is called.

The `send_payload` for non-LMStudio is `{"prompt": payload.get("prompt", "")}` - that's a dict. But `send_payload` for LMStudio is the entire `payload` - also a dict.

Wait, let me look at `session.send()`. Looking at the LMStudioSession code:Now let me check the CopilotClient session `send` method. The error is `'str' object has no attribute 'get'` - it's happening in `stream_agent_response` when the CopilotClient session's `send` is called. 

Looking at line 710: `await session.send(send_payload)` where `send_payload = {"prompt": payload.get("prompt", "")}`. That's a dict, so it shouldn't be a string.

Wait, let me look at this more carefully. The error says `'str' object has no attribute 'get'`. 

Could it be that `session.send()` for CopilotClient is receiving a dict but somewhere internally it's treating it incorrectly? Or could it be that `MODEL_MAPPING.get(...)` is returning a string that gets passed as `model_id`, and that string is then used as a `config` dict somewhere?
 
I'm tracing through the flow: `MODEL_MAPPING.get()` returns a string model ID, which gets passed to `stream_agent_response`, then used to create a copilot session with a config dict containing the model ID and streaming flag. So the config dict structure looks right, but somewhere the model ID string might be getting treated as if it were a config object.

Looking at `_create_copilot_session`, it tries three different approaches to handle SDK version differences: first passing the config items as kwargs, then passing the entire dict as a positional argument for older SDKs, and finally falling back to no arguments. The second strategy of passing `session_kwargs` as a dict should work with the old SDK format.

Now I'm tracing through where the error occurs—after `_create_copilot_session` returns a session object, `session.send()` is called with a payload. The error `'str' object has no attribute 'get'` suggests either the session itself is a string or the send method is receiving a string instead of a dict, which would indicate one of the fallback strategies is returning something unexpected. I'm realizing the issue might be in how the CopilotClient SDK's `send` method processes the payload—it could be expecting a string directly rather than a dict, and somewhere internally it's calling `.get()` on what it receives, which would fail if a string is passed instead. So the string itself is what's being passed to `.get()` somewhere in the call chain. I'm realizing this might not be a pure Python SDK issue at all—the version `0.0.411` suggests this is the GitHub Copilot CLI tool, and `from copilot import CopilotClient` is importing from that CLI binary, which could explain why a string is being treated as a dict somewhere in the internal processing. I'm tracing where the `.get()` call is failing by working backwards through the code path. The error suggests something is being treated as a string when it should be a dict, but I need to figure out exactly where in the event handling chain this mismatch occurs. The error is actually happening inside `stream_agent_response`, not in the session creation—it's occurring right before that function returns. I need to trace through what's happening in there rather than focusing on the SDK's `create_session` method, since the old SDK version might be handling the arguments differently than I expected. I'm realizing the issue might be in the MODEL_MAPPING lookup—if that dictionary is empty, the fallback "gpt-5-mini" would be used, but the log shows "claude-haiku-4.5" is actually being selected, which suggests the mapping either has values or the model_id is coming from somewhere else in the flow.

Looking at how the copilot session gets created, I'm setting up the configuration with the model_id and streaming enabled, then passing the system prompt if one exists before initiating the session. Looking at the CopilotClient SDK version v0.0.411, I need to verify whether `session.send()` accepts a dict with a `prompt` key or just a plain string. The real issue might be in how `handle_event` processes the response—if the newer SDK returns a dict instead of an object with attributes, the current code could fail when trying to access event properties. I'm realizing the issue—`hasattr(event.type, 'value')` would actually raise an AttributeError before `hasattr` can even evaluate it if `event` doesn't have a `type` attribute, but the outer try/except would catch that. The real problem is that the error message says `'str' object has no attribute 'get'`, which isn't an AttributeError at all, so something else is going on with how the event is structured. I'm seeing a pattern of `.get()` calls on `payload` throughout the function, which would all fail if `payload` were a string instead of a dictionary. Now I need to trace back to where `stream_agent_response` is actually being called to understand what type `payload` should be.Let me look at the `build_payload` function and the `stream_agent_response` function more closely to find where the error is:Now I understand the full picture. The `build_payload` returns a dict with `"prompt"`, optionally `"images"`, and `"system_prompt"`. Then `stream_agent_response` receives this dict as `payload`.

Inside `stream_agent_response`, for CopilotClient:
```python
system_prompt = payload.get("system_prompt", "")
copilot_config = {"model": model_id, "streaming": True}
if system_prompt:
    copilot_config["system_message"] = system_prompt
session = await _create_copilot_session(client, copilot_config)


```

Then I'm passing a dict with just the prompt to `session.send()`. The error `'str' object has no attribute 'get'` is likely coming from inside the CopilotClient SDK itself—the `session.send()` method probably expects a plain string rather than a dict, which would explain why it's trying to call `.get()` on what it receives. I need to track down where the `CopilotClient` library is coming from and what its actual implementation looks like to understand what's happening with the `send()` method.Now let me look at the `client.py` to understand `create_session`:I can see the issue now! Look at line 489:
```python
on_permission_request = cfg.get("on_permission_request")
```

`cfg` is the config dict. But wait - the current code in `_create_copilot_session` does:

Strategy 1:
```python
return await client.create_session(
    on_permission_request=_default
